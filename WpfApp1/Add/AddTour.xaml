<Window x:Class="WpfApp1.AddTour"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddTour">

    <Grid>
        <Label Content="Путевка"/>
        <MenuItem Header="Добавить путевку" Command="{Binding CreateTour}"/>
        <ComboBox ItemsSource="{Binding TourArray}" 
                  SelectedItem="{Binding SelectedTour}"
                  DisplayMemberPath="Title" Margin="0,-41,0,0"/>
        <Button Content="Сохранить" Command="{Binding SaveValue}"/>
    </Grid>
    </Window>